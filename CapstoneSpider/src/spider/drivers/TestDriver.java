package spider.drivers;

public class TestDriver {

	/*public static void main(String[] args) {
		
		SpiderRobot bot;
		SpiderChromosome chrom;
		BotHandler handle = new BotHandler();
		long start = System.currentTimeMillis();
		double avgDist = 0;
		for (int i = 0; i < 64; i++) {
			bot = new SpiderRobot();
			chrom = new SpiderChromosome(1, 0);
			handle.makeRun(chrom, bot);
			System.out.println("" + bot.getCurrDist() + " " + bot.getAngleScoreModifier() + " " + bot.getBalanceError());
			avgDist += bot.getCurrDist();
		}
		System.out.println("On average, bots in this generation move " + (avgDist / 64) + " meters over 1000 timesteps");
		System.out.println("Execution time for single generation: " + ((double) (System.currentTimeMillis() - start) / 1000) + " seconds");
	} */

}
